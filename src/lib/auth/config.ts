import { authOptions } from './auth-options'

// Re-export authOptions as authConfig for consistency
export const authConfig = authOptions

// Export authOptions directly for API routes
export { authOptions }